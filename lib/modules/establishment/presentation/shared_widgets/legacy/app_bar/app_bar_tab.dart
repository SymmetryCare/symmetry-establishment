import 'package:flutter/material.dart';
///app bar for commuincation tab screen
class AppBarTab extends StatelessWidget {
  const AppBarTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
